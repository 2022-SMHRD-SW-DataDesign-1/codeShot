<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codeshot.mapper.PostMapper">

	<!-- 찜목록 검색, 김지수 -->
	<select id="wishList" parameterType="String" resultType="WishListDTO">
		select B.post_num, B.post_category, B.mem_email, B.post_title, B.post_price
		from tbl_wishlist A, tbl_post B 
		where (A.post_num = B.post_num) 
			and (A.mem_email = #{email})
	</select>
</mapper>